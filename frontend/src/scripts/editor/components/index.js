import { Ctrl as Area } from "./Area.vue";
import { Ctrl as Check } from "./Check.vue";
import { Ctrl as Input } from "./Input.vue";
import { Ctrl as Options } from "./Options.vue";

const Control = {
  Area,
  Check,
  Input,
  Options,
};

export default Control;
